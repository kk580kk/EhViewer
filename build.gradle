// 根构建已由 HarmonyOS hvigor 接管，此文件仅保留以免 Gradle 报错。
// 主构建请使用: npm run build 或 hvigorw assembleHap
// 原 Android 构建配置已弃用，见 build.gradle.bak（若需恢复可重命名）

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
